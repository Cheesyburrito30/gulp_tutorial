{
  "name": "tasks",
  "version": "1.0.0",
  "description": "Lets take a look at what Gulp tasks are, the pieces that make them up, and how to build a simple task.",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^4.1.0"
  },
  "dependencies": {
    "bootstrap": "^4.5.0"
  }
}
