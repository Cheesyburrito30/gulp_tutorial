{
  "name": "public",
  "version": "1.0.0",
  "description": "Public Gulp tasks are the functions that we export to and call with Gulp CLI. These tasks tell Gulp what order to run private tasks, and what order to run them in. These tasks are defined by `series(...tasks)`, `parallel(...tasks)`, or a single private task.",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-dart-sass": "^1.0.2",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  }
}
